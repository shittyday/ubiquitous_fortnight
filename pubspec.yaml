name: ubiquitous_fortnight
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.1
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  webview_flutter: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  custom_lint:
  riverpod_lint: ^1.1.5
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

